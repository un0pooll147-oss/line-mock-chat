# v75 修正内容

ベース：ローカルの最新版 `line-mock-chat-scene2-varied-profile-icons-v72.zip`

## チャット履歴

『乾杯』（仮）1.1.3.pdfの第2話を参考に、上司・龍馬（右／自分）と妻・佳菜子（左／相手）の23件の会話を作成しました。脚本の逐語転記ではなく、夫婦の距離感を踏まえた撮影用の創作履歴です。

- 期間：2026/09/15〜2026/09/18
- 最新日付・新規送受信の日付：2026/09/18
- 画面時刻：19:15
- 後輩を誘うことへのためらい、佳菜子の後押し、龍馬が誘えた報告、当日の「今日はご飯いらない」の連絡を反映
- 最新時点ではコンペ開始前です。結果を示す会話は入れていません

旧作品の既定設定が初回表示を上書きしないよう、本作品用の既定設定保存先を分けました。旧データを削除せず、保存済みチャットは共通で維持します。本作品で新たに保存した設定は従来どおり読み込めます。

## 全5モードの表示

- OFF：周囲16pxの余白がある通常プレビュー（最大幅448px、最大高さ860px）
- ON：ブラウザ内の表示領域いっぱいに広がる擬似フルスクリーン
- ブラウザのFullscreen APIは呼ばないため、ブラウザの案内バーは発生しません
- 設定をOFFにするかEscapeキーで通常表示に戻せます
- デバイスフレームは独立設定として維持しています
- 共通処理を `components/usePseudoFullscreen.ts` にまとめました
- `components/useNativeFullscreen.ts` は削除対象です。既存プロジェクトへ上書きする場合は、この旧ファイルを削除してください

## 確認結果

- TypeScript型チェック成功
- 本番ビルド成功
- ローカルブラウザで5モードのOFF→ON→OFFを実操作し、位置と寸法の変化を確認
- TikTokの端末フレーム併用、Escape復帰、PC幅での切り替えを確認
- Android／iPhone実機と公開サイトへの反映は未確認です。公開作業は行っていません

## 起動方法

Node.js環境で `npm install` 後、`npm run dev` を実行します。本番用は `npm run build` 後、`npm start` を実行します。

`node_modules` とビルド生成物はZIPに含めていません。

## v75 追加

- チャットモードの「画面・通知」設定に「入力中メッセージの文字サイズ」を追加
- 80%〜200%の範囲で5%刻みに変更可能
- 下部の入力欄と、送信演出中の吹き出しの両方へ反映
- 入力文が複数行になる場合は、文字サイズに合わせて入力欄の高さも自動調整
- 既定設定と保存チャットにも値を保存し、読み込み・初期化へ対応
